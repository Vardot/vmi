{#
/**
 * @file
 * View modes inventory - Text teaser - small text only template.
 */
#}
{% if title_suffix.contextual_links is not null %}
  {{ title_suffix.contextual_links }}
{% endif %}
<{{ outer_wrapper }}{{ attributes.addClass('text-teaser-view-mode', 'small-text-only', 'clearfix') }}>
  <{{ main_wrapper }} class="main-content">
    {{ content.main }}
  </{{ main_wrapper }}>
</{{ outer_wrapper }}>

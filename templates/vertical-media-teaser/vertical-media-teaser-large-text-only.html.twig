{#
/**
 * @file
 * View modes inventory - Vertical media teaser - large text only template.
 */
#}
<{{ outer_wrapper }}{{ attributes.addClass('vertical-media-teaser-view-mode', 'large-text-only', 'clearfix') }}>
  {% if title_suffix.contextual_links is not null %}
    {{ title_suffix.contextual_links }}
  {% endif %}
  <{{ main_wrapper }} class="main-content">
    {{ content.main }}
  </{{ main_wrapper }}>
</{{ outer_wrapper }}>
